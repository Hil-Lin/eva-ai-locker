# F 盘已有资源清单

> **路径**: `F:\进阶篇之-基于RK3588的AI模型训练到部署\`
> **记录日期**: 2026-06-11
> **作用**: 跨对话记忆，下次可以直接告诉 Claude 这个文件里有这些资源

---

## 目录结构

```
F:\进阶篇之-基于RK3588的AI模型训练到部署\
│
├── ⭐ DeepSeek-R1-Distill-Qwen-1.5B_W8A8_RK3588.rkllm
│      大小: 2,035,400,092 字节 (~1.9GB)
│      说明: DeepSeek-R1-Distill-Qwen-1.5B，W8A8量化，已编译为RK3588格式
│      用途: 直接放到 ELF 2 上推理，不需要再做任何转换
│
├── ⭐ rknn-llm-main/                              ← Rockchip RKLLM SDK 完整仓库
│   │
│   ├── rkllm-runtime/                             ← C/C++ 运行时（板子端推理用）
│   │   └── Linux/librkllm_api/
│   │       ├── aarch64/librkllmrt.so              ← ARM64 运行时动态库（核心！）
│   │       └── include/rkllm.h                    ← C API 头文件
│   │
│   ├── rkllm-toolkit/                             ← Python 工具包（仅 PC 端，模型转换用）
│   │   ├── rkllm_toolkit-1.1.4-cp38-cp38-linux_x86_64.whl
│   │   └── rkllm_toolkit-1.1.4-cp310-cp310-linux_x86_64.whl
│   │   注意: 这两个 wheel 是 x86_64 的，不能在 ARM 板子上装
│   │
│   ├── rknpu-driver/
│   │   └── rknpu_driver_0.9.8_20241009.tar.bz2    ← NPU 内核驱动（板子上可能已装）
│   │
│   ├── examples/DeepSeek-R1-Distill-Qwen-1.5B_Demo/ ← 完整 Demo
│   │   ├── Readme.md                              ← 使用说明
│   │   ├── deploy/
│   │   │   ├── install/demo_Linux_aarch64/         ← 预编译交付物
│   │   │   │   ├── llm_demo                       ← ARM64 可执行文件
│   │   │   │   └── lib/librkllmrt.so              ← 运行时.so（与上面相同）
│   │   │   ├── src/llm_demo.cpp                   ← Demo 源码
│   │   │   ├── build-linux.sh                     ← 编译脚本
│   │   │   └── CMakeLists.txt
│   │   └── export/                                ← 模型导出脚本
│   │
│   ├── doc/
│   │   └── Rockchip_RKLLM_SDK_CN_1.1.4.pdf        ← SDK 中文文档
│   │
│   ├── scripts/
│   ├── CHANGELOG.md
│   └── README.md
│
├── AI部署源码/                                     ← 空目录
│
├── AI 应用部署手册.pdf           (6,878,472 字节)
│
└── 基于RK3588的AI模型训练到部署.pdf (9,266,694 字节)
```

---

## Person A 需要的关键文件

| 文件 | 位置 | 用在哪 |
|------|------|--------|
| `librkllmrt.so` | `rknn-llm-main/examples/.../deploy/install/demo_Linux_aarch64/lib/` | 板子上的动态库 |
| `rkllm.h` | `rknn-llm-main/rkllm-runtime/Linux/librkllm_api/include/` | C API 头文件（写 ctypes 封装用） |
| `llm_demo` | `rknn-llm-main/examples/.../deploy/install/demo_Linux_aarch64/` | 预编译 demo（先跑通验证） |
| `llm_demo.cpp` | `rknn-llm-main/examples/.../deploy/src/` | 源码（理解 API 调用方式） |
| `.rkllm 模型` | 根目录 | 最终在板子上推理的模型 |

## Person B 需要的关键文件

| 文件 | 用途 |
|------|------|
| `rknpu_driver_0.9.8_20241009.tar.bz2` | 如果板子 NPU 没驱动，需要安装 |

## ~~不需要的文件~~

| 文件 | 原因 |
|------|------|
| `rkllm_toolkit-*.whl` | 是 x86_64 的，用于 PC 端模型转换，部署时不需要 |

---

## 板子信息（串口连接）

| 项目 | 值 |
|------|-----|
| 登录用户 | `elf` 密码 `elf`，或 `root` 密码 `root` |
| 串口工具 | Putty，COM口（设备管理器查看实际编号），115200 波特率 |
| 系统 | Buildroot Linux，aarch64 |
| 内核 | Linux 5.10.209 |
| Python | 需要检查 `python3 --version` |
| 网络 | 有网口，可配 IP 后 scp 传文件 |
